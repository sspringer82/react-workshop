# Aufgabe 3

Jeder Eintrag in der Liste erhält einen Button.
Der Klick auf den Button soll das jeweilige Objekt aus der Liste (und dem State löschen).

`onClick={(event) => { /*magic */ }}`
`setState((prevState) => {return newState })`

Bonus: Frage vor dem Löschen, ob sich der Benutzende wirklich sicher ist.
